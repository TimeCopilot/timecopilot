### Fixes

* **TabPFN LOCAL tests**: Cast `unique_id` to string in TabPFN integration test fixtures so LOCAL forecasts pass with `tabpfn-time-series>=1.2.0` item_id dtype validation.

### Dependencies

* Bumped `foundationforecast` from `>=0.1.6` to `>=0.1.7` (TabPFN `item_id` string coercion in the wrapper).

---

**Full Changelog**: https://github.com/TimeCopilot/timecopilot/compare/v0.0.33...v0.0.34
